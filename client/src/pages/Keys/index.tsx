import { Layout } from '../../components';

const Keys = () => {
  return (
    <Layout>
      <header className="my-4">
        <span className="text-title">Keys</span>
      </header>
    </Layout>
  );
};

export default Keys;
